    lodsw
    cmp {r[i]}, rax
    jae .default ;this is an unsigned jge. handles the negative case nicely
    mov ax, word [rsi + {r[i]}]
    
    .default:
    add rsi, rax
    sub rsi, 2
    ;wait for dispatch to do the rest