    lodsw
    mov rbx, [registers+{i}*8]
    mov rcx, [rsi + rax - 4]
    cmp rbx, rcx
    jg .gt
    je .eq
    ;less than:
    movsx rbx, word [rsi]
    jmp .end
    .eq:
    movsx rbx, word [rsi+2]
    jmp .end
    .gt:
    movsx rbx, word [rsi+4]
    .end:
    lea rsi, [rsi + rbx - 4]