    lodsw
    mov rbx, [registers+{i}*8]
    movsx rax, ax
    cmp rbx, rax
    jae .default ;unsigned jge

    mov rax, rbx
    
    .default:
    lea rsi, [rax * 2 + rsi - 4]

    .dispatch:
    xor rax, rax